syntax = "proto3";

package pdytr.example.grpc;

message Request {}
message Response {}
// Defining a Service, a Service can have multiple RPC operations
service Service {
  // Define a RPC operation
   rpc hacerAlgo(Request) returns (Response);
}

